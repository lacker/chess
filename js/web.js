import React from 'react';  

var board = require("")

let App = React.createClass({  
  render() {
    return (
      <div>
        hello react world!
      </div>
    );
  }
})

React.render(<App/>, document.body);


